# Development and profiling dependencies (not required for normal use).
# Install with:  pip install -r requirements-dev.txt

-r requirements.txt

# Profiling
snakeviz>=2.2
memory_profiler>=0.61

# Testing
pytest>=7.4
pytest-cov>=4.1
